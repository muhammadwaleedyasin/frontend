/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['firebasestorage.googleapis.com', 'cdn.webhotelier.net','images.interhome.com'], 
  },
};

export default nextConfig;
